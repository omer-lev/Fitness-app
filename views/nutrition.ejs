<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu - Fitness App</title>

    <link rel="stylesheet" href="/styles/nutrition.css">
    <%- include('partials/includes.ejs') %>
</head>
<body>
    <%- include('partials/header.ejs') %>

    <div class="menu">
        <div class="title">
            <h2><%= menu.title %></h2>
            <br>
            <button id="restButton">ימי מנוחה</button>
            <button id="workButton">ימי אימון</button>
        </div>

        <div class="meal">
            <img src="/img/arrow_left.png" id="left-arrow">

            <div class="container">
                <h3>ארוחה 1</h3>

                <p dir="rtl"><%= menu.work[0] %></p>
            </div>

            <img src="/img/arrow_right.png" id="right-arrow">
        </div>

        <a href="/">back</a>
    </div>


    <script>
        let workMenu = `<%= menu.work %>`.split(",");
        let restMenu = "<%= menu.rest %>".split(",");
    </script>

    <script src="/scripts/nutrition.js"></script>
</body>
</html>